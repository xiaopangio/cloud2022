server:
  port: 80
eureka:
  client:
    register-with-eureka: false
    service-url:
        defaultZone: http://myeureka7001.com:7001/eureka/
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
